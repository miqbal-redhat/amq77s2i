#!/bin/sh
# Called during the image build life cycle.

SRC_CONFIG="/tmp/src/configuration"
DEST_CONFIG="/opt/amq/conf"



if [ -d $SRC_CONFIG ]; then
    echo "Copying config files..."
    chmod +x ${DEST_CONFIG}/*
    cp $SRC_CONFIG/* $DEST_CONFIG/
    echo "Done"
fi
